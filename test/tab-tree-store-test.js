import { expect } from 'chai'

export function describeTabTreeStoreTests() {
  describe("Array", function () {
    describe("#indexOf()", function () {
      it("should return -1 when the value is not present", function (done) {
        expect(1).to.equals(1);
        done();
      });
    });
  });
}
